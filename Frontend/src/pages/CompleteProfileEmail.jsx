import React,{useState} from 'react';import axios from 'axios';import {useNavigate} from 'react-router-dom';
export default function CompleteProfileEmail(){const nav=useNavigate();const [email,setEmail]=useState('');const [err,setErr]=useState('');const submit=async(e)=>{e.preventDefault();try{const base=localStorage.getItem('apiBaseUrl')||import.meta.env.VITE_API_URL||'http://localhost:5000';const phone=localStorage.getItem('organizerPhone');await axios.post(`${base}/api/auth/complete-profile-email`,{phone,email});localStorage.setItem('organizerEmail',email);nav('/dashboard');}catch(ex){setErr(ex.response?.data?.message||'Update failed');}};return <div className="min-h-screen flex items-center justify-center"><form onSubmit={submit} className="bg-white p-6 rounded shadow w-96"><h2 className="text-xl font-bold mb-2">Email Required</h2><p className="text-sm mb-3">Dashboard access के लिए ईमेल अपडेट करें।</p><input type="email" required className="w-full border p-2 mb-3" value={email} onChange={e=>setEmail(e.target.value)} placeholder="Email"/><button className="w-full bg-blue-600 text-white p-2 rounded">Save Email</button><p className="text-red-500 mt-2">{err}</p></form></div>}